{% import "_includes/forms" as forms %}

{{ forms.textField({
    first:        true,
    label:        "Path to Manifest file"|t,
    id:           'manifestFile',
    name:         'manifestFile',
    instructions: "Path to the generated manifest file (including filename)",
    value:        settings.manifestFile,
    errors:       settings.getErrors('manifestFile'),
    required:     true
}) }}

<hr>

<h3>Template Usage</h3>

<p>Find a versioned CSS asset.</p>
<code>
    <pre>
{{ '<link rel="stylesheet" href="{{ craft.elixir.version(\'css/app.css\') }}">' | e }}
    </pre>
</code>

<p>Find a JavaScript versioned asset.</p>
<code>
    <pre>
{{ '<script src="{{ craft.elixir.version(\'js/app.js\') }}"></script>' | e }}
    </pre>
</code>

<p>Lazily find a versioned asset and build a default tag. This will output the correct tag based on the file extension.</p>
<code>
    <pre>
{{ '{{ craft.elixir.withTag(\'js/app.js\') | raw }}' | e }}
    </pre>
</code>
